let initialState = {
    landings: [
        'red',
        'blue',
        'indigo'
    ]
}

const landingPageReducer = (state = initialState, action) => {
    return state;
}

export default landingPageReducer;